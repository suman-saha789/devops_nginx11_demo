#Devops Nginx demo

This projrct helps you install nginx on Linux

#Steps

sudo apt-get update

sudo apt install nginx

sudo systemctl install nginx

sudo systemctl enable nginx

sudo systemctl status nginx
